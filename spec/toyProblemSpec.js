describe('Toy Problems', function() {

  describe('bubble sort', function() {
    it('should return an array', function() {
      //call bubble sort function with array
      // create expected variable
      let actual = true;
      let expected = true;
      expect(actual).to.be.eql(expected);
    });
  });
});