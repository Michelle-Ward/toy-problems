class Node {
  constructor(value) {
    this.value = value;
    this.next = null;
  }
}

class SinglyLinkedList {
  constructor(){
    this.head = null;
    this.tail = null;
    this.length = 0;
  }

  push(value) {
    //create a new node with value
    //if head and tail are null
      //reassign both to the new node
    //else
      //reassign the current tail's next to the new node
      //reassign tail to the new node
  }
}
